# 项目协作说明

## 项目概览

这是一个中文单页待办事项应用，页面标题为“今日清单 · Todo List”。项目是**零构建步骤的静态前端**：直接在浏览器中打开 `index.html` 即可运行，不依赖包管理器、后端服务或测试框架。

## 文件与职责

| 文件 | 职责 |
| --- | --- |
| `index.html` | 页面语义结构、可访问性属性、外部字体和 CSS/JS 引用。 |
| `app.js` | 任务状态、渲染、筛选、编辑、浏览器本地存储，以及空状态鼓励语逻辑。 |
| `styles.css` | 主题变量、响应式布局、组件状态和交互样式。 |

## 技术与运行方式

- 原生 HTML、CSS 与 JavaScript（无模块、无框架）。
- 使用 Google Fonts：Manrope、Noto Sans SC、DM Mono；网络不可用时会回退到系统字体。
- 可直接用浏览器打开 `index.html`，或用任意静态文件服务器预览。
- 当前目录不是 Git 仓库；不要假定可使用 Git 历史、分支或提交来追溯改动。

## 核心行为

### 任务

- 初次加载时展示 `defaultTasks` 中的示例任务；之后从 `localStorage` 读取任务。
- 新任务置顶，文本去除首尾空白，长度上限为 80 个字符。
- 支持勾选完成、删除、双击任务文本编辑。
- 编辑时：Enter 保存，失焦保存；Escape 放弃本次编辑并重新渲染。
- 支持“全部 / 进行中 / 已完成”筛选和“清除已完成”。
- 顶部圆环、筛选数量和底部摘要均基于全部任务实时计算。

### 鼓励语与空状态

- 当前筛选结果为空时显示空状态；即使任务仍存在，筛选结果为空也会展示。
- “换一句”只在内置 `encouragements` 数组中循环，不保存选择。
- 用户自定义鼓励语保存后优先展示，且下次刷新仍保留；保存后标题固定为“给自己的一句话”。

## 本地存储约定

不要随意修改以下键名；若必须迁移数据，应提供兼容读取或迁移逻辑。

| 键名 | 数据格式 |
| --- | --- |
| `day-flow-tasks-v1` | JSON 数组，元素为 `{ id, text, done }`。 |
| `day-flow-encouragement-v1` | 自定义鼓励语的纯文本。 |

`loadTasks()` 已对 JSON 解析异常做回退处理；修改存储逻辑时应保留故障降级能力。

## 修改约定

- 文案、日期格式与界面语言保持简体中文；HTML 根节点使用 `lang="zh-CN"`。
- 任务文本插入 HTML 前必须经过 `escapeHtml()`，避免将用户输入作为 HTML 执行。新增任何渲染用户输入的路径也应遵循这一点。
- 每次修改任务状态后通过 `render()` 统一更新 DOM 和持久化，不要仅手动修改局部计数或样式导致状态不同步。
- 保持现有 CSS 自定义属性作为颜色和主题的单一来源，优先复用 `--accent`、`--ink`、`--muted` 等变量。
- 现有移动端断点为 `520px`；调整布局时需兼顾该断点下的卡片内边距、按钮和页脚显示。
- 保留已有的可访问性设计：表单标签、`aria-label`、任务列表的 `aria-live` 和键盘可用的原生按钮。

## 验证建议

手动在浏览器验证以下流程：

1. 新增空白任务与正常任务，确认空白输入不会新增任务。
2. 切换完成状态、筛选、删除和清除已完成，确认数量、摘要和进度环同步更新。
3. 双击编辑，分别验证 Enter、失焦、Escape。
4. 刷新页面，确认任务和自定义鼓励语保持；清除浏览器站点数据后确认回到示例任务。
5. 检查桌面与窄屏（不大于 520px）下的布局和按钮可操作性。
